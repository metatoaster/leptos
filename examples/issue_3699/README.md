# Issue 3699 demo.

This is the complete demo that demonstrates the hydration issue noted in
the original issue.

## Reproduction

Run `cargo leptos watch` to first start the server, then point a browser
to `http://localhost:4000/foo` and see the hydration issue.
